import 'package:flutter/material.dart';
import 'package:flutter_hooks/flutter_hooks.dart';

class GunsListPage extends HookWidget {
  const GunsListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const GunsListView();
  }
}

class GunsListView extends HookWidget {
  const GunsListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
